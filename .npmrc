# jest-environment-jsdom declares an optional peer on canvas@^2.5.0, but the CLI
# needs canvas@^3 for AVIF/WebP alpha support. The versions are incompatible as
# peers, so allow the modern canvas to satisfy the (optional) jsdom peer.
legacy-peer-deps=true
